[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --strict-markers --tb=short
markers =
    payments: тести платіжних потоків
    idempotency: перевірки повторних callbacks
    concurrency: паралельні запити
